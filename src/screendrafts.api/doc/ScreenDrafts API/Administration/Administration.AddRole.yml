info:
  name: Administration.AddRole
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/admin/roles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
