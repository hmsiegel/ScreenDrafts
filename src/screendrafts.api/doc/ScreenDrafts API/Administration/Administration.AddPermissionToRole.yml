info:
  name: Administration.AddPermissionToRole
  type: http
  seq: 3
  tags:
    - Administration

http:
  method: POST
  url: "{{baseUrl}}/admin/roles/:roleName/permissions/:permission"
  body:
    type: json
    data: |-
      {
        "publicIds": []
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 Response
    description: No Content
    request:
      url: "{{baseUrl}}/admin/roles/:roleName/permissions/:permission"
      method: POST
      body:
        type: json
        data: |-
          {
            "publicIds": []
          }
    response:
      status: 204
      statusText: No Content
      body:
        type: text
        data: ""
  - name: 400 Response
    description: Bad Request
    request:
      url: "{{baseUrl}}/admin/roles/:roleName/permissions/:permission"
      method: POST
      body:
        type: json
        data: |-
          {
            "publicIds": []
          }
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
  - name: 401 Response
    description: Unauthorized
    request:
      url: "{{baseUrl}}/admin/roles/:roleName/permissions/:permission"
      method: POST
      body:
        type: json
        data: |-
          {
            "publicIds": []
          }
    response:
      status: 401
      statusText: Unauthorized
      body:
        type: text
        data: ""
  - name: 403 Response
    description: Forbidden
    request:
      url: "{{baseUrl}}/admin/roles/:roleName/permissions/:permission"
      method: POST
      body:
        type: json
        data: |-
          {
            "publicIds": []
          }
    response:
      status: 403
      statusText: Forbidden
      body:
        type: text
        data: ""
  - name: 404 Response
    description: Not Found
    request:
      url: "{{baseUrl}}/admin/roles/:roleName/permissions/:permission"
      method: POST
      body:
        type: json
        data: |-
          {
            "publicIds": []
          }
    response:
      status: 404
      statusText: Not Found
      body:
        type: text
        data: ""
  - name: 500 Response
    description: Internal Server Error
    request:
      url: "{{baseUrl}}/admin/roles/:roleName/permissions/:permission"
      method: POST
      body:
        type: json
        data: |-
          {
            "publicIds": []
          }
    response:
      status: 500
      statusText: Internal Server Error
      body:
        type: text
        data: ""
