info:
  name: Administration.GetPermissionByCode
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/admin/permissions/:code"
  params:
    - name: code
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
