info:
  name: Administration.GetUserRoles
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/admin/users/:publicId/roles"
  params:
    - name: publicId
      value: u_dOqN66QaK5XUEQ3
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
