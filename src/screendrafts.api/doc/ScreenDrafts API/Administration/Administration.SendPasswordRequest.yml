info:
  name: Administration.SendPasswordRequest
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/admin/users/:publicId/password-reset"
  params:
    - name: publicId
      value: u_bVcnKnwEkZgmqW5
      type: path
  body:
    type: json
    data: "{}"
  auth: inherit

runtime:
  variables:
    - name: publicId
      value: "u_bVcnKnwEkZgmqW5\t"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
