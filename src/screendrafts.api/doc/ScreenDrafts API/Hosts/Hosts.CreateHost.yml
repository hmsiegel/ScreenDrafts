info:
  name: Hosts.CreateHost
  type: http
  seq: 3
  tags:
    - Hosts

http:
  method: POST
  url: "{{baseUrl}}/hosts"
  body:
    type: json
    data: |-
      {
        "personPublicId": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    description: Created
    request:
      url: "{{baseUrl}}/hosts"
      method: POST
      body:
        type: json
        data: |-
          {
            "personPublicId": ""
          }
    response:
      status: 201
      statusText: Created
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 400 Response
    description: Bad Request
    request:
      url: "{{baseUrl}}/hosts"
      method: POST
      body:
        type: json
        data: |-
          {
            "personPublicId": ""
          }
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
  - name: 401 Response
    description: Unauthorized
    request:
      url: "{{baseUrl}}/hosts"
      method: POST
      body:
        type: json
        data: |-
          {
            "personPublicId": ""
          }
    response:
      status: 401
      statusText: Unauthorized
      body:
        type: text
        data: ""
  - name: 403 Response
    description: Forbidden
    request:
      url: "{{baseUrl}}/hosts"
      method: POST
      body:
        type: json
        data: |-
          {
            "personPublicId": ""
          }
    response:
      status: 403
      statusText: Forbidden
      body:
        type: text
        data: ""
