info:
  name: Predictions.GetDraftPartPredictions
  type: http
  seq: 8
  tags:
    - Predictions

http:
  method: GET
  url: "{{baseUrl}}/draft-parts/:draftPartId/predictions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/draft-parts/:draftPartId/predictions"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 401 Response
    description: Unauthorized
    request:
      url: "{{baseUrl}}/draft-parts/:draftPartId/predictions"
      method: GET
    response:
      status: 401
      statusText: Unauthorized
      body:
        type: text
        data: ""
  - name: 403 Response
    description: Forbidden
    request:
      url: "{{baseUrl}}/draft-parts/:draftPartId/predictions"
      method: GET
    response:
      status: 403
      statusText: Forbidden
      body:
        type: text
        data: ""
  - name: 404 Response
    description: Not Found
    request:
      url: "{{baseUrl}}/draft-parts/:draftPartId/predictions"
      method: GET
    response:
      status: 404
      statusText: Not Found
      body:
        type: text
        data: ""
