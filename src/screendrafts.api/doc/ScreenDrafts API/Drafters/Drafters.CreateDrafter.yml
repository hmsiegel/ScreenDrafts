info:
  name: Drafters.CreateDrafter
  type: http
  seq: 2
  tags:
    - Drafters

http:
  method: POST
  url: "{{baseUrl}}/drafters"
  body:
    type: json
    data: |-
      {
        "personId": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    description: Created
    request:
      url: "{{baseUrl}}/drafters"
      method: POST
      body:
        type: json
        data: |-
          {
            "personId": ""
          }
    response:
      status: 201
      statusText: Created
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 400 Response
    description: Bad Request
    request:
      url: "{{baseUrl}}/drafters"
      method: POST
      body:
        type: json
        data: |-
          {
            "personId": ""
          }
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
  - name: 401 Response
    description: Unauthorized
    request:
      url: "{{baseUrl}}/drafters"
      method: POST
      body:
        type: json
        data: |-
          {
            "personId": ""
          }
    response:
      status: 401
      statusText: Unauthorized
      body:
        type: text
        data: ""
  - name: 403 Response
    description: Forbidden
    request:
      url: "{{baseUrl}}/drafters"
      method: POST
      body:
        type: json
        data: |-
          {
            "personId": ""
          }
    response:
      status: 403
      statusText: Forbidden
      body:
        type: text
        data: ""
  - name: 409 Response
    description: Conflict
    request:
      url: "{{baseUrl}}/drafters"
      method: POST
      body:
        type: json
        data: |-
          {
            "personId": ""
          }
    response:
      status: 409
      statusText: Conflict
      body:
        type: text
        data: ""
