info:
  name: Drafters.SearchDrafters
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/drafters/search"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
