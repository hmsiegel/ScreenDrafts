info:
  name: DraftParts.GetTriviaResults
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/draft-parts/:publicId/trivia-results"
  params:
    - name: publicId
      value: dp_CYVmCtMNvLJBa1M
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
