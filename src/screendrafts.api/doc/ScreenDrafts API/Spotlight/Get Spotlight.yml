info:
  name: Get Spotlight
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/spotlight"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
