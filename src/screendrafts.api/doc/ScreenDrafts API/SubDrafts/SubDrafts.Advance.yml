info:
  name: SubDrafts.Advance
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/draft-parts/:draftPartId/sub-drafts/:subDraftId/advance"
  params:
    - name: draftPartId
      value: ""
      type: path
    - name: subDraftId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
