info:
  name: 22 Pick 5 - Maddie
  type: http
  seq: 22

http:
  method: POST
  url: "{{baseUrl}}/draft-parts/{{draftPartId}}/picks"
  body:
    type: json
    data: |-
      {
        "position": 5,
        "playOrder": 3,
        "participantPublicId": "{{maddieParticipantId}}",
        "participantKind": 0,
        "moviePublicId": "m_rNnVHw4q7L9KzeL"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(201);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
