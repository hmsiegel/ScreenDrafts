info:
  name: 15 Assign Participant To Draft Position - Maddie
  type: http
  seq: 15

http:
  method: PUT
  url: "{{baseUrl}}/draft-parts/{{draftPartId}}/positions/{{drafterBPosition}}/participant"
  body:
    type: json
    data: |-
      {
        "participantPublicId": "{{maddieDrafterId}}",
        "participantKind": 0
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(204);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
