info:
  name: 30 Set Status Complete
  type: http
  seq: 30

http:
  method: PUT
  url: "{{baseUrl}}/drafts/{{draftPublicId}}/parts/1/status"
  body:
    type: json
    data: |-
      {
        "action": 2
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: expect(res.getStatus()).to.equal(200);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
